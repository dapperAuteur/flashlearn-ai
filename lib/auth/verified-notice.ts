// Set by /api/auth/verify-email on success, read by the /verified page. A notice, not a credential:
// it carries no identity, only "this browser just completed a verification".
export const VERIFIED_NOTICE_COOKIE = "flashlearn_verified_notice";
